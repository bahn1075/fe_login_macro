# npm 설정 파일
# 펀딩 메시지 비활성화
fund=false

# 보안 감사 비활성화 (CI/CD에서만)
audit=false

# 업데이트 알림 비활성화
update-notifier=false

# 진행률 표시 비활성화 (CI/CD 환경에서 로그 정리)
progress=false

# deprecated 경고 억제
silent=true

# 로그 레벨 설정 (error만 출력)
loglevel=error
